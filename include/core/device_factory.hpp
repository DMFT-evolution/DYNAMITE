struct DeviceSimulationData;

DeviceSimulationData* createDeviceSimulationData();
void destroyDeviceSimulationData(DeviceSimulationData*);

struct DeviceRKData;

DeviceRKData* createDeviceRKData();
void destroyDeviceRKData(DeviceRKData*);